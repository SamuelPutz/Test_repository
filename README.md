# The 'hle_dev_test_sam.rb' file is ruby file which is used to generate cleaned names and sentences and fill 'clean_name' and 'sentence' fields in 'hle_dev_test_sam_putz' table in applicant_tests DB.
